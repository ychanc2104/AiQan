name: Deploy Canary

on:
  push:
    branches: [canary]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-canary
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: canary
      url: https://ychanc2104.github.io/AiQan/canary/
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build
        working-directory: frontend
        env:
          GITHUB_ACTIONS: 'true'
          DEPLOY_ENV: canary
        run: npm run build

      - name: Deploy to GitHub Pages (canary subfolder)
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: frontend/out
          target-folder: canary
          clean: true
